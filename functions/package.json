{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "check": "node --check index.js && node --check test/rules.test.js",
    "test:rules": "firebase emulators:exec --only firestore \"jest test/rules.test.js\""
  },
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.0",
    "jest": "^29.7.0"
  },
  "private": true
}
